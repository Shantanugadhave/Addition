// Online C++ compiler to run C++ program online
#include <iostream>

int main() {
    int a, b,add;
    cout<<enter number<<endl;
    cin>>a;
    cout<<enter number<<endl;
    cin>>b;
    sum=a-b;
    cout<<"subtraction"<<sum<<endl;

    return 0;
}
